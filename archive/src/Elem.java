public class Elem {
   /* Participant head;
    private int lenght;
    Elem next;

    Elem() { }

    public void put(Participant newParticipant) {
        newParticipant.next = head;
        head = newParticipant;
    }

    public void sortElem(Elem firstElem, Elem secondElem) {
        Participant newParticipant = new Participant();
        while (newParticipant.next != head) {
            if (firstElem.head.compareTo(secondElem.head) > 0) {
                newParticipant = firstElem.head;
                firstElem.head = firstElem.head.next;
            }
            if (secondElem.head.compareTo(firstElem.head) < 0) {
                newParticipant = secondElem.head;
                secondElem.head = secondElem.head.next;
            }
            this.put(newParticipant);

            newParticipant = newParticipant.next;
        }
    }*/
}